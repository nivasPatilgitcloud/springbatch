CREATE TABLE IF NOT EXISTS foodtb(
    id VARCHAR(255) PRIMARY KEY,
    date VARCHAR(255),
    region VARCHAR(255),
    city VARCHAR(255),
    category VARCHAR(255),
    product VARCHAR(255),
    qty VARCHAR(255),
    unit_price VARCHAR(255),
    total_price VARCHAR(255)
);
